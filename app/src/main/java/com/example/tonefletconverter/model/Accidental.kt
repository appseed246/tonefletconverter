package com.example.tonefletconverter.model

/**
 * 変化記号
 */
enum class Accidental {
    /**
     * 元の音(♮)
     */
    Natural,

    /**
     * 半音上(♯)
     */
    Sharp,

    /**
     * 半音下(♭)
     */
    Flat
}